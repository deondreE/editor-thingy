package core

// @Goal: The idea is that interface should just generate index and vertex buffers that are univeral to any rendering backend.
// So then we don't have to think about windows, linux, macos UI

